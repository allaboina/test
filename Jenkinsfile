pipeline {
    agent any

    stages {
        stage('Git Verification') {
            steps {
                echo "✅ GitHub integration working successfully!"
                sh 'git rev-parse --abbrev-ref HEAD'
            }
        }

        stage('Build Placeholder') {
            steps {
                echo "🏗️ This is a placeholder for future changelog automation steps."
            }
        }
    }
}
